apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: knowledge-base
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: frontend